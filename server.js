const express = require('express');
const app = express();

const PORT = 3001;
// auth/register
// auth/login
// auth/forgot-password



app.listen(PORT, () => {
    console.log("Server is running at the port", PORT);
});
